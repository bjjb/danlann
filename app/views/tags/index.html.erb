<% title "Tags" %>

<ul class='tags'>
  <% linearize(@tags).each do |popularity, tag| %>
    <% content_tag_for 'li', tag, :class => "popularity#{popularity}" do %>
      <%= link_to tag.name, tag %>
    <% end %>
  <% end %>
</ul>
